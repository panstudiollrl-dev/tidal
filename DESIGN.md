# DESIGN.md — Tidal 聲音引擎設計

> 規範「要做什麼」：訊號流、握力球對應、潮汐狀態機、海浪 DSP、spatial 設計、參數表。
> 與 `AGENTS.md`（怎麼做事）搭配看。所有數值是**建議起點**，須在真實握力球上校準；改動請更新本表並在 `AGENTS.md` 交接紀錄註明。

---

## 1. 整體訊號流

```
[Ball 1 grip+IMU] ──┐
                    ├─► 校正/平滑 ─► 互動層(有界 recipe) ─► 參數 ─► 海浪合成器 ─► 空間化 ─► soft-clip ─► 輸出
[Ball 2 grip+IMU] ──┘                (方向/潮汐狀態機)         (Farnell 模型)   (Panner/Convolver)  (tanh)
```

- **READ**：兩顆球的 grip（連續主控）+ IMU shake（離散事件）。協定見 `GRIPBALL_PROTOCOL.md`。
- **ENGINE**：有界 recipe——把身體輸入映射到一組永遠成立的參數，不自由生成。
- **WRITE**：程序式海浪聲場 + 空間定位。
- **MEASURE**：聆聽時間、握壓次數/節律、前後 0–10 緊張度（沿用 nature_loop 的 session 流程與 CSV）。

### 1.1 節奏分析框架（2026-07-09）

Tidal 的概念語言更新為：**節奏調和系統**。設計目標不是把使用者推向單一「放鬆」狀態，而是幫助使用者恢復節奏彈性：能感覺緊、能發力，也能回到可呼吸、可停留的海。

對應 Lefebvre 的 rhythmanalysis（詳見 `RHYTHMANALYSIS.md`）：

| 節奏關係 | 在 Tidal 中的設計含義 |
|---|---|
| Polyrhythmia 複節奏 | 保留多層 LFO、握力、揮動與聲場的差異，不急著同步成單一節拍。 |
| Eurhythmia 和諧節奏 | 使用者能停留、呼吸、塑形；聲音逐漸更寬、更穩、更少強事件。 |
| Arrhythmia 失序節奏 | 硬握、劇烈揮動或可能的高喚起訊號出現時，系統承認能量但加邊界，把能量導入較慢潮汐。 |
| Isorhythmia 等節奏 | 反模式：避免固定節拍、分數化、催促式 haptic 或把使用者強制帶到單一節奏。 |

EEG 若接入，先作為低權重慢變節奏層與探索性量測；不要把 EEG 當命令或診斷。

## 2. 兩顆球的角色

> **控制置換（Pan 2026-07-09）**：兩顆球的握壓角色互換。**Ball 1 grip＝潮汐能量；Ball 2 grip＝鵝卵石捲動（＋聲場空間）。** 方向仍由揮動（swing）決定，任一球皆可。以下表已反映置換後狀態。

### Ball 1 — 潮汐 Tide（grip）
控制「海的能量」——漲退潮與拍岸強弱的主軸。

| 輸入 | 映射 | 邊界 / 行為 |
|---|---|---|
| grip level 0→1 | tide energy：湧浪頻率↑、浪高↑、拍岸亮度↑、整體變近 | 全部有上限；最大值仍是「豐沛的海」而非噪音牆 |
| 放鬆 | tide 降到低喚起底床：慢、遠、低沉，但持續 | 不歸零、不靜音 |

### Ball 2 — 鵝卵石 / 空間（grip）
控制「退浪時大圓石捲動的量」（主控，Pan 實測 Ball1 原本的聲場微調不顯著，改到這裡更可感），另微調聲場遠近/開闊為輔。

| 輸入 | 映射 | 邊界 / 行為 |
|---|---|---|
| grip level 0→1 | 鵝卵石捲動量：握越用力，退浪時石頭滾動越多越響（呼應阿朗壹大圓石灘）；另微調聲場開闊 | 有界；退浪相位（swell 低相位）給滾動節奏；放鬆時石頭近靜、聲場變廣而遠 |
| 放鬆 | 石頭近靜、聲場變廣而遠 | 永不指向單一刺耳點 |

### 方向 Direction（揮動 swing，任一球）
**方向由揮動決定：海浪跟著球揮動的方向跑。** 揮動當下平滑帶動 azimuth；停止揮動後緩慢回中偏廣。

### 浪拍礁石（rock crash）— 需「用力握 + 揮動」雙條件（Pan 決策 2026-07-08）
**只有在「握得很用力」AND「有明顯揮動」同時成立時**才觸發海濤拍石強拍事件。
單純揮動、或單純用力握，都不觸發——維持放鬆底床。

| 條件 | 結果 |
|---|---|
| grip level ≥ 硬握閾值 **且** swing ≥ 強揮閾值 | 觸發一次浪拍礁石（稀疏、限幅）＋**強觸覺脈衝** |
| 其一不成立 | 不觸發強拍，只有跟隨揮動的海浪移動 |

> 設計意圖：Ball 2＝潮汐在呼吸；Ball 1＝用揮動把海推向某方向。放鬆時兩者都仍是好聽的海；要「發力」才會出現拍岸的戲劇性，符合放鬆優先、強互動需刻意觸發。

### 觸覺回饋 Haptic（Pan 決策 2026-07-08）
球支援震動（`cmd_id 11`，見 `GRIPBALL_PROTOCOL.md`）。行為規則：

| 情境 | 觸覺 |
|---|---|
| 一般 arrival / session / 揮動 / 拍石 | **不震動**。目前全域 `HAPTICS_ENABLED=false`，避免零碎、無意義的震動干擾沉浸。 |
| 4-7-8 手動握拍 | 使用者每主動握一下球，給一次**短確認回饋**（`sendHapticAll(..., true)`，繞過全域開關）。這不是系統自動排程，而是「我收到你這一下」的觸覺確認。 |
| 放下、無明顯揮動 | **不震動**（維持放鬆、不打擾） |

觸覺送出要節流（每 ~100ms 最多一次）避免藍牙塞車。除非 Pan 再明確改決策，不要恢復自動 haptic pattern。

## 3. 潮汐狀態機 / 巨觀動態

海不是單一穩態，而是**多個不同週期的慢 LFO 疊加**（見 Farnell 的「wave sets」概念，`RESEARCH.md` §2）：

- `swell`：主湧浪週期，8–16 s（受 Ball 2 能量影響：能量高→週期短、浪高）。
- `set_slow`：更慢的「浪組」包絡，30–90 s，讓一陣陣大浪自然來去，不機械重複。用 2–3 個**不可公約**週期的 LFO 相加避免聽出循環。
- `foam_gate`：泡沫/嘶聲由湧浪包絡的上升段開啟，退浪時淡出。

**浪拍礁石事件（rock crash）**：**觸發條件＝grip level ≥ 硬握閾值 且 swing ≥ 強揮閾值**（見 §2）。單次事件 = 一個 chirp/bandpass 噪音爆破 + 快起慢落包絡 + 短殘響尾巴，空間上定位在當前揮動方位，並同步送出一次強觸覺脈衝。務必稀疏（最小間隔）、限幅。不做高能量時的自發觸發——強互動一律需刻意發力。

## 4. 海浪 DSP（Farnell 海浪/水體模型，見 RESEARCH.md §2）

分三層，加總後空間化再限幅：

1. **底床 surge（海床呼吸）**：寬頻噪音 → 低通/帶通濾波，截止頻率與音量由 `swell` × tide energy 調變。這是「一整片海」的持續體，永不消失。
2. **foam / hiss（泡沫嘶聲）**：高通噪音，由 `foam_gate`（湧浪上升段）開閤，量隨 tide energy 增。給浪的「碎裂」細節。
3. **impacts（浪拍/水花）**：離散事件。可用 Farnell 的 bubble/water-drop 模型（正向 chirp 正弦 + 衰減包絡）做水花點綴，及 bandpass 噪音爆破做拍岸。稀疏觸發。
4. **鵝卵石沿岸 shingle（退浪石頭滾動）**（Pan 2026-07-09）：預生成「密集短促噪音撞擊」的循環紋理（每 1.5–17ms 一顆、3–13ms 快衰減、立體聲去相關），過 bandpass（~1.6–3kHz，能量高→更亮）塑成石質。由**退浪閘門**（`swell` 低相位＝水退時最盛）× tide energy 調變，與 foam（湧上相位）形成呼應。這是本聲景的定位聲——鵝卵石灘的沙沙／喀啦，是高辨識度的自然聲源（呼應 `AESTHETIC.md` §1.1 sound object recognition）。

所有濾波器截止、音量、寬度都用 `setTargetAtTime` 平滑。加總 → `tanh` soft-clip → destination。

> **更新（Pan 2026-08-04）：底床的噪音源改用粉紅噪音，不用白噪音。**
> Pan 的回饋是「這個版本的模仿海浪的白噪音很假」。原因不只在濾波器：白噪音每倍頻程
> 的能量相同，聽起來是「嘶」；阿朗壹的錄音是 −3dB/oct 的自然斜率（實測頻帶分布見
> `assets/hrir/README.md`）。`surge` / `foam` 現在都吃 `pinkNoiseSource()`（Voss-McCartney
> 一階近似的 IIR），實測 250Hz→4kHz 落 −10.4dB／4 個倍頻程。
> ⚠️ 這件事**必須用實測頻譜驗**，不能只檢查係數在不在：「係數留著、但輸出那一行改成
> `d[i] = w`」名字、係數、註解全對，聽起來還是白的。驗收在
> `tmp/check_alangyi_match.js [3]`（Goertzel DFT + 1/3 倍頻程平均）。
>
> 同時 `SHIMMER_LEVEL = 2.4`、`PEBBLE_FLOOR = 0.14`——Pan 回饋「shimmer 和 pebble
> 的聲音幾乎都是沒有的」。pebble 的 floor 是讓退浪石頭聲**不會完全歸零**（守
> 「聲音永遠成立、永不消失」）。
>
> **更新（Pan 2026-08-05）：`pebbleBuffer` 整段重寫，新增 `PEBBLE_RATE = 11`。**
> Pan 試聽：「pebble 的聲音品質非常差」。實測舊版有三個問題，前兩個都是「寫錯不會報錯、
> 只會聽起來很差」的那種：
> ① **循環接縫有喀聲**：舊版 `while(i < len)` 一路往前撒，最後一顆石頭的尾巴被 `len` 硬切，
> 而 pebble 是 `loop = true` ⇒ **每 4.5 秒聽到一次喀**。實測接縫的跳變比它鄰域（±5ms）內
> 任何跳變都大（最壞 3.28×，27% 的接縫 >1.0）。改法：每顆石頭起點獨立亂數，寫入用
> `(start + j) % len` **繞回開頭**，尾巴接到頭上 ⇒ 最壞降到 0.64×、0% 超過 1.0。
> ② **密度太高糊成嗡嗡**：舊版每 8–38ms 一顆＝~34 顆/秒，crest 只有 14.9dB（幾乎沒有瞬態
> 對比）＝聽不出「一顆一顆石頭」。改成 `PEBBLE_RATE = 11` 顆/秒、每顆 26–84ms ⇒ crest 21dB。
> （`AESTHETIC.md` 提到的「石頭撞擊 ~47/s」是**整個海灘**的錄音密度，不是單一循環紋理該有的。）
> ③ 每顆缺了**互撞的那一下**：加一個 8ms 快衰減的中頻分量（650–1800Hz）當接觸瞬態。
> 它大部分會被下游 `pebbleBP` 濾掉（那是刻意的「被水吃掉高頻」），留下的一點是「石頭」
> 而不是「低頻噗」的關鍵線索。
> ⚠️ **量測點很重要**：`pebbleBP` 是 LP 380–800Hz，所以「500–2000Hz 幾乎沒有能量」是
> **設計**而不是缺陷；在濾波器**之前**量會得到完全相反的結論（我第一次就量錯了，
> 差點把修法定成「補中頻」）。驗收在 `tmp/check_pebble_quality.js`（接縫／密度／crest／
> 過濾波器後的頻帶），變異測試 `tmp/mutate_pebble_quality.js`。
>
> **更新（Pan 2026-08-06）：「請把一開始的滾石聲完整去除」⇒ 底量改成開場淡入。**
> `PEBBLE_FLOOR = 0.14` 是**常駐**的，所以頁面一開聲就有滾石紋理——而開場（`arrival`）
> 這時使用者還沒握到任何東西，那個聲音沒有任何對應的來源，只是背景噪。
> ⚠️ **但不能直接把 `PEBBLE_FLOOR` 刪成 0**：那會同時撞到兩條既有決定——「聲音永遠成立、
> 永不消失」（guardrail），以及 Pan 2026-08-04 自己回報的「pebble 的聲音幾乎都是沒有的」。
> **改法：底量不是被刪掉，而是「還沒被打開」。** 新增 `pebbleFloorAmt`（初值 **0**）乘在
> `PEBBLE_FLOOR` 上，使用者**第一次真的握下去**（`level >= MANUAL_478_ON`，在 `setSpread`
> 裡偵測）才把它打開，然後用 `PEBBLE_FLOOR_RAMP_S = 6.0` 秒的時間常數慢慢長到滿——慢到
> 聽不出「被打開」的那一刻，守「平滑，不跳變」。
> 握出來的 `0.52 × stoneAmt` **不乘**這個係數，所以第一次握球當下就有石頭聲（否則變成
> 「第一次握沒反應」＝製造出另一個 Pan 剛回報過的症狀）。
> 驗收在 `tmp/test_grip_thresholds.js [5]`＋`tmp/mutate_grip_thresholds.js`（含「初值改成 1」、
> 「底量沒乘上係數」、「淡入變成瞬間打開」、「連握出來的量也被擋住」四個變異）。

## 5. 空間化 Spatial Audio

**目標**：讓「方向」與「遠近」可被身體直接感覺，強化沉浸與放鬆。

**播放情境（Pan 2026-07-08）：耳機 或 智能眼鏡**——皆頭戴式個人音訊，故走 binaural / HRTF；不需喇叭串音消除。開放式智能眼鏡的外化感受不同，實機再微調 wet/dry 與距離（見 `RESEARCH.md` §6）。

**決策（Pan 2026-07-08）：第一版就上 MeshRIR convolution。** 兩級同時存在，但 MeshRIR 是目標，PannerNode 只作為 IR 就緒前的暫時退路。

- **方位層**：揮動方向 → 方位；tide energy → 距離（近/遠）。提供動態的方向移動感。
- **空間著色層（MeshRIR ConvolverNode，第一版必做）**：把海的各層（或方位層輸出）送進以 **MeshRIR** 實測室內脈衝響應建的 `ConvolverNode`，得到真實房間的空間著色與沉浸包覆。不同接收點的 IR 對應聽者在聲場中的位置。IR 匯出步驟見 `RESEARCH.md` §3、`assets/ir/README.md`。
- 選配：需要多點聲場或 6DoF 時再評估 ambisonics（`Omnitone` / `JSAmbisonics`）。

> **更新（Pan 2026-08-04）：方位層已從 `PannerNode(HRTF)` 換成實測 HRIR 卷積。**
> Pan 在 `web/spatial_bench.html` 做 A/B/C 盲測後選了 C 路（實測 HRIR），Omnitone（B 路）未選用。
> IR 在 `assets/hrir/`（水平面 90 個方位角），實作見 `web/index.html` 的 `HrirBank` / `HrirSource`，
> 來源與量測見 `assets/hrir/README.md`，決策脈絡見 `AGENTS.md` 2026-08-04 (b)(c)。
>
> **兩件必須一起記住的事：**
> ① 這批 IR 低頻往下斜（220Hz 掉 −23.8dB），要在**卷積之後**接一段固定等化鏈
> （`HRIR_TILT_FIX` + `HRIR_TILT_TRIM`）把傾斜本身拉平，否則等於悄悄改掉依阿朗壹錄音
> 調好的平衡——Pan 2026-08-04 聽到的「白噪音很假、shimmer 和 pebble 幾乎沒有」就是這個。
> **不要用每層一個寬頻倍數去補**：傾斜是頻率的函數，寬頻倍數只能拉回該層的中位能量，
> 層內斜度還在（`surge` LP800 的 100Hz 與 700Hz 仍差 15dB）。有了等化鏈之後
> `SPATIAL_MAKEUP` 全部塌回 ~1.0（0.98–1.03），量測與驗收數字見 `assets/hrir/README.md`。
> ② `shimmer` / `sub` / `wide` **刻意不過方位層**——它們是「一片」而不是點聲源。
> 一次性音效（impact / cue / glint）目前仍用內建 `PannerNode`，理由見交接紀錄。
>
> **更新（Pan 2026-08-05）：shimmer 有自己的乾濕比。** Pan 試聽：「海的空間感還不錯，
> 只是 shimmer 的聲音太遠了，幾乎感覺不到」。遠近感是 **direct-to-reverberant ratio**，
> 不是音量——全域是 dry 0.4 / wet 0.8（殘響大於直達聲，−6.0dB），所以每一層都被推遠，
> 光調大 `SHIMMER_LEVEL` 只會得到「一個比較大聲的遠處的東西」。改法是給 shimmer 自己一對
> gain（`SHIMMER_DRY = 2.2`、`SHIMMER_WET = 0.40`）：D/R 從 −6.0dB 轉成 **+8.8dB**，
> 而送到 clip 的**總量維持 1.20（0.0dB）**——這兩個數字是一起挑的。
> ⚠️ **不可以拿音量換距離感**（例如只把 wet 調小）：Pan 的抱怨正是「幾乎感覺不到」。
> 驗收在 `tmp/check_alangyi_match.js [4]`（同時斷言 D/R 轉正**與**總能量不減，
> 並把 shimmer 的匯流權重納入頻帶計算，否則那支測試會對這個改動視而不見）。
> 仍留 `SHIMMER_WET > 0.1`：全乾會變成貼耳的乾硬電子音。
>
> **來源與授權（Pan 2026-08-05 指示「請查詢」，已查證確認）**：`assets/hrir/` 這批 IR 是
> **ARI（Acoustics Research Institute, Austrian Academy of Sciences）HRTF database,
> subject `nh2`**，授權 **CC BY-SA 3.0**，引用 Majdak, Goupell & Laback (2010),
> *Atten Percept Psychophys* 72, 454–469。**不是 MeshRIR、也不是 SADIE**——這兩個舊說法
> （duck-hunt 的 `.gitignore` 寫 MeshRIR、`gripball_webhid.js` 註解寫 SADIE-style）都是錯的。
> ⚠️ **share-alike 是新的限制**：`assets/ir/room.wav` 的 MeshRIR 是 CC BY 4.0（沒有 SA），
> 兩者條款不同，**不要混著寫**；對外發佈的處理方式待 Pan 決定。
> 證據鏈（格線／維度／頻譜指紋／排除法）與「為什麼波形比對量不出來」見
> `assets/hrir/README.md`。
>
> 訊號鏈：sources → **makeup gain → 實測 HRIR 卷積 → HRIR_TILT_FIX 等化鏈**(方位/距離) → MeshRIR convolver(空間著色) → soft-clip → out。
> HRIR 未載入時自動退回內建 `PannerNode(HRTF)`（聲音永遠成立）；頁面 **H** 鍵可即時 A/B。
> 若房間 IR 尚未匯出，`convolver` 先旁路（bypass）。

## 6. 參數表（建議起點，待校準）

| 參數 | 範圍 | 平滑時間常數 | 由誰控制 |
|---|---|---|---|
| azimuth（方位） | −90°…+90°（無揮動緩回 0） | 揮動即時帶動、回中 3–5 s | Ball 1 揮動方向 |
| listener spread（聲場寬） | 窄…廣 | 3 s | Ball 1 grip（放鬆變廣） |
| tide energy | 0…1 | 3–6 s | Ball 2 grip |
| swell period | 8–16 s | — | tide energy |
| set envelope | 30–90 s（多 LFO 疊加） | — | 自走 |
| surge lowpass cutoff | 400–3000 Hz | 1–2 s | tide energy × swell |
| foam gain | 0…上限 | 0.5–1 s | foam_gate × tide |
| **`GRIP_FULL_SCALE`（固定滿刻度）** | **1400 raw**（相對零點；`HEADROOM 1.22` ⇒ effScale 1708） | — | 固定常數，不學習 |
| **硬握閾值**（觸發拍石） | **747 raw**（`gripLevelForRaw(747)` ⇒ level 0.44 @1400） | — | 拍石雙條件之一 |
| **強揮閾值**（觸發拍石 / 揮動偵測） | swing mag ≥ 校準值 | — | 拍石雙條件之一 |
| impact 最小間隔 | ≥1.5 s | — | 硬性 guard |
| impact 峰值 | 限幅（tanh 前 ≤ 設定上限） | 快起(≤30ms)慢落(0.5–1.5s) | 硬握+強揮 |
| **haptic 自動震動** | 目前關閉 | 節流 ~100ms | `HAPTICS_ENABLED=false`；不要自動排程震動 |
| **4-7-8 手動握拍短震** | 短確認 | 節流 ~100ms | 使用者每主動握一下才觸發，`force=true` |
| master soft-clip | tanh | — | 固定保護 |

> **`GRIP_FULL_SCALE`：900 → 1400（Pan 2026-08-05）。** Pan 回報「從自我覺察呼吸這邊水位
> 顯示就很糟糕了」。900 是**照 `GRIPBALL_PROTOCOL.md` 的「firm grip ≈ +1250 raw」推**的，
> 不是量真球量出來的——而 Pan 那兩顆真球大約是它的兩倍。重跑他 291 秒的紀錄（只用 `raw`
> 與 `tMs`）：刻意握一下的 dev 峰值 **1009–2831 raw**，而舊的 effScale 只有 1098 ⇒ 正常握
> 一下就超過滿刻度，breath 段峰值 0.98/0.66/0.98/0.81＝幾乎次次貼頂，水位只剩「0 或滿」。
> 改成 1400 後同一份紀錄的 breath 峰值是 0.40–0.84、貼頂 2.6% → 0%，兩顆球仍都到得了高水位。
> ⚠️ **這件事污染了已記錄的資料**：`record/tidal_record_2026-08-05T05-19-01.json` 的
> `arrival.pre_tension = 9` 是貼頂造成的，那個資料點不可信。
> ⚠️ **不要改成「每顆球自己學天花板」**：試過（只長不縮、連續超過就抬），它會收斂到偶發的
> 最大尖峰（ball1 1494／ball2 2807），於是每一次正常的握只剩 0.1–0.4，「用握力表達緊張」
> 那題會被記成 0 分。學錯比不學更糟——與 2026-08-04 移除 cue 校正同一個理由。
> 詳細量測與取捨見 `GRIPBALL_PROTOCOL.md`，驗收在 `tmp/sim_grip_rezero.js [8]`。

> **⚠️ 所有握力門檻一律用「力道（raw）」定義，不可以寫成裸水位數字（Pan 2026-08-06）。**
> 這是上面那次 `GRIP_FULL_SCALE` 900 → 1400 的**後座力**，也是這個專案在同一個地方踩的第二次坑。
> 水位是**正規化**的：`level = pow((|dev|/effScale − 死區)/(1−死區), GAMMA)`，
> `effScale = GRIP_FULL_SCALE × GRIP_HEADROOM`。所以每個寫成水位的門檻，它的**實際力道意義
> 是被 `GRIP_FULL_SCALE` 決定的**。滿刻度一改 1400，所有門檻在同一秒被悄悄調高 **1.55 倍**，
> **一行程式碼都沒有被改到，diff 上完全看不出來**：
>
> | 門檻 | 原本寫法 | span 900 時的力道 | span 1400 之後變成 | 現在的寫法 |
> |---|---|---|---|---|
> | `AFTER_ON` | `0.24` | 296 raw | **460 raw** | `gripLevelForRaw(300)` |
> | `AFTER_OFF` | `0.07` | ~205 raw | ~271 raw | `gripLevelForRaw(250)` |
> | `ARRIVAL_PRESS_ON` | `0.28` | 330 raw | 513 raw | `gripLevelForRaw(330)` |
> | `ARRIVAL_PRESS_OFF` | `0.16` | 234 raw | 364 raw | `gripLevelForRaw(234)` |
> | `MANUAL_478_ON` | `0.20` | 264 raw | 411 raw | `gripLevelForRaw(264)` |
> | `HARD_GRIP` | `0.70` | 747 raw | 1163 raw | `gripLevelForRaw(747)` |
> | 分級「有一點／很明顯」 | `0.33` / `0.66` | 373 / 703 raw | **581 / 1094 raw** | `gripLevelForRaw(380 / 700)` |
>
> **這正是 Pan 2026-08-06 回報的兩個症狀，而且是同一個 bug：**
> ① 「478 結束之後握力球就沒有反應了」＝結束後問卷的 `AFTER_ON` 變成 460 raw。重播 Pan 的
> 實機紀錄：`after` 段能連續達標的最長時間只有 **875ms**，而記下一個答案需要
> `AFTER_HOLD_MS = 1100ms` ⇒ **問卷在物理上答不出來**，畫面就是沒反應。
> ② 「有一顆球幾乎沒有效用」＝兩顆 MB01（同 vendorId/productId）的靈敏度天生差約 **2 倍**
> （刻意握的 dev 中位數 ball1 **382** / ball2 **780** raw），460 raw 正好切在兩者之間；
> 而「很明顯」的 1094 raw 是弱球用力握也到不了的 ⇒ 那顆球永遠只能答最低一級。
> **修法**：加一個 `gripLevelForRaw(raw)`（與 `GripCalibrator` 用同一條曲線），門檻全部寫成
> 原本設計時真正想要的力道值。`GRIP_FULL_SCALE = 1400` **不動**（降它會把上面那個水位動態
> 的修法弄回去：實測 FS 900–1100 讓貼頂率從 0.9% 回升到 5.1%）。
> 修好之後同一份紀錄：`after` 段 875ms → **2118ms**（1.9 倍餘裕）、**兩顆球各自單獨都答得出**
> （ball1 2118ms／ball2 1608ms）、兩顆都表達得到三個級別、貼頂率仍 <1%。
> ⚠️ **不可以改成「每顆球一組自己的門檻」**：slot 1／2 只是**連線順序**，不對應特定那顆球
> （誰先連上就是 1）。理由同上面「不要每顆球自己學天花板」。
> 驗收在 `tmp/test_grip_thresholds.js`（107 項，含用 Pan 實機紀錄重播的 AE-1／AE-2 驗收）
> ＋ `tmp/mutate_grip_thresholds.js`（46/46）。其中 `[2b]` 是關鍵守門：它把 `GripCalibrator`
> 裡的造型算式抽出來當真值，逐點比對 `gripLevelForRaw`——**兩邊的曲線一旦分岔，門檻的力道
> 意義就會第三次悄悄偏掉，而兩段程式各自看起來都完全合理。**

> **壞球信任判定：`gripTrust` → `gripStuckSince`（Pan 2026-08-05 指示「要」修）。**
> 2026-07-22 加過一層防護：一顆卡在高位不會放開的壞球（phantom）不該代表使用者作答，
> 所以問答那條路走 `trustedHeld()` 而不是原始水位。但它的狀態記的是「**上次放開的時刻**」、
> 初值 **0**，於是「還沒有任何證據」被當成「**在時間 0 就放開過**」。`performance.now()`
> 是從**開頁**起算的 ⇒ **頁面開超過 25 秒之後才連上的球，第一個 report 就被判成不可信**，
> `trustedHeld()` 回 0。而讀說明、按連線、戴耳機本來就要花幾十秒，所以這是**常態**而非例外。
> ⚠️ **這就是 Pan 2026-08-04 說「只有小小回顧的段落的握力跟水位關係是對的」的真正原因**：
> 那一段不是對，是**被歸零蓋掉**了——它是唯一走 `trustedHeld()` 的地方。而水位**顯示**
> 走的是 `Math.max(state.grip…)`（沒經過信任判定），所以同一次 session 會出現
> 「水位是滿的、但回顧那段完全沒反應」。
> ⚠️ 更糟的是它**安靜地損壞資料**：兩顆都不可信 ⇒ held 恆 0 ⇒ 作答窗逾時自動跳、
> `agreement` / `pre_tension` 被記成 0，畫面上看不出任何異常。
> **改法：語意從「上次放開多久前」換成「卡住多久了」**——`gripStuckSince[slot]`，`null` ＝
> 沒有卡住的證據 ⇒ **給信任**；高於 `AFTER_OFF` 才開始計時（只記第一次跨過的時刻，
> 不能每幀重設）；一回到低位立刻清成 `null`；`forgetBallSlot` 也要清（換球不繼承）。
> 窗口 `GRIP_TRUST_MS = 25000` 不變，所以 07-22 那個 phantom 防護完整保留。
> 驗收在 `tmp/check_grip_trust.js`（27 項）＋ `tmp/mutate_grip_trust.js`（13/13）。
> 這一項**特別**需要變異測試，因為壞掉的時候畫面上看不出來（水位照樣滿），純聽純看驗不到。
> ⚠️ **這條路只有 zh 頁有**：en 頁沒有 `trustedHeld`／`armAgreement`，它直接用
> `Math.max(state.grip[1], state.grip[2])` ⇒ 本來就沒有這個症狀。要不要把整套（連同
> `AFTER_ON` 0.24 vs en 0.14 的差異）搬到 en，是 Pan 的決定，見 `AGENTS.md`。

> **一段握壓怎麼變成一個答案：`AnswerSampler`（Pan 2026-08-06）。**
> Pan：「**用握力回答各種問題 目前球都太敏感 輕輕碰就全滿 也很容易滑動去其他區域**」。
> ⚠️ 這個抱怨最直覺的讀法是「把門檻抬高」，而那是**錯的**——上面那條 AE 的修法才剛把門檻
> 從 460 raw 還原成 300 raw，就是為了修 Pan 的另一個抱怨「有一顆球幾乎沒有效用」。
> 兩個抱怨都是真的，因為成因不同：**敏感度的來源不是門檻，是「怎麼從一段握壓算出一個數」。**
> 用真的 `GripCalibrator` 重播 Pan 2026-07-22 的紀錄，量到兩個獨立成因：
> ① **峰值定案**：舊碼用 `a.peak`／`agreePeak`（這段握壓的最高點）。真球的 dev p99 是
>    811(ball1)／2372(ball2)、max 1643／3103——一次「輕輕碰」裡本來就有幾幀尖峰，而峰值定案
>    讓**任何一幀**都足以決定整題 ⇒ 實測 ball2 有 **30% 的刻意握被記成 10/10**（貼頂）。
> ② **換級歸零**：舊碼 `if(a.band !== band){ a.heldMs = 0 }`。水位在分級邊界上下游移是常態
>    （實測一次握之中換級**中位 2 次、最多 18 次**），每換一次計時就重來 ⇒ 使用者感覺
>    「滑動去其他區域」而且答不出來（實測 ball2 43 次刻意握有 **33 次湊不滿 1100ms**）。
> **修法**：`class AnswerSampler`（無 DOM／無時鐘依賴，可以直接 `new` 來對拍）取代兩處各自
> 手寫的計分，三件事：**滯後計時**（≥`AFTER_ON` 進、只有 <`AFTER_OFF` 才中斷，**分級變化
> 不再影響計時**）＋**跳過前 `AFTER_SETTLE_MS = 300`**（手還在往目標爬，那一段不是答案）
> ＋定案取**中位數**而不是峰值。
> ⚠️ 為什麼是**中位數**不是平均：平均擋不住 3103 raw 那種量級的尖峰。實測「380 raw 輕握
> ＋ 一次 3103 尖峰」中位數 0.161 →「沒有」（對），平均 0.217 →「有一點」（錯）。
> 第一版 probe 用時間加權平均，**讓 Pan 的症狀變得更嚴重**。
> 結果：**13/13 個穩定力道的分級與改版前完全相同**（AE 的弱球可達性一分不減），
> 但「輕握 ＋ 一次尖峰」從貼頂變回「沒有」、ball2 貼頂率 30% → 0%。
> 水位顯示也改走 `display()`（＝目前的中位數），所以畫面跟著「會被記下來的那個值」走，
> 不會被一幀尖峰推到全滿再慢慢退——這就是 Pan 看到的「全滿」。
> 順帶收掉一個舊分岔：「握多久才算」原本有**兩個**常數（`AFTER_HOLD_MS` 與
> `AGREEMENT_FIX_MS`），而 `AFTER_HOLD_MS` 在 zh 是 1100、en 是 900。兩者都移除，現在
> 兩頁只有 `AFTER_SAMPLE_MS = 1100` 一個數字。
> 驗收在 `tmp/test_answer_sampler.js`（172 項，含兩頁一致性與**行為**一致性）
> ＋ `tmp/mutate_answer_sampler.js`（66/66）。
> ⚠️ 為什麼需要一支專門的測試：Pan 回報這個症狀時，**同一段程式碼通過了 106 項斷言**——
> 因為那些測試各自寫了一份自己的計分規則，頁面上真正在算分的那段沒有任何測試看著。
> 所以新測試（以及 `test_survey_recovery.js` / `test_grip_thresholds.js`）一律用 regex
> 把頁面上**真正的** `AnswerSampler` 抽出來跑，不在測試裡重寫一份。

## 7. Session 流程

`arrival`（抵達）→ `session`（聆聽、握、探索）→ `after`（再評）→ `summary`。

Arrival 不是技術校正畫面，而是自然對話式身體入口：

1. **左右手自然對應（默默校正）**：依序請使用者拿左手/右手；中央球亮起時按住、暗下時放開，各三次。實作必須吃 **rising-edge**：球亮起前已按住不算，必須先放開、亮後再按。畫面不使用「校正」等技術語言。
2. **30 秒呼吸覺察**：不引導使用者改變呼吸；請自然呼吸，**吸氣時握著，吐氣時放下**。記錄吸/吐狀態變化與間距 pattern，不只看平均。
3. **1.2 秒緊張程度握持**：畫面文字為「用握力表達緊張的程度」。使用者用握力表示此刻緊張、情緒或能量強度；系統記錄施力曲線，並同時更新握力範圍。
4. **小 report + 使用者校正**：用非診斷語言給一句綜合身心印象，請使用者用握力表示貼近程度；再依第一段自我檢測建議呼吸場景。

紀錄：聆聽時長、握壓次數/節律、Arrival features、前後緊張度、舒適度、再使用意願。寫本機 CSV，欄位匿名。
第一個比較實驗：**固定海浪聲景 vs 握力球可控版（同素材）**。EEG 僅探索性，不作放鬆證明。

### 7.0.2 接手警示：任何「等使用者放開」的閘門都必須有逾時（Pan 2026-08-06）

> Pan：「478過程中因為沒有持續按壓握力球 連線就消失了 導致呼吸結束之後 整個問卷都不能
> 繼續做而卡住」。這是**三個各自獨立**的缺陷疊在一起，每一個都足以單獨造成同一個畫面
> （一顆不動的球），而且**畫面上完全看不出在等什麼**。

| # | 缺陷 | 為什麼會發生 | 修法 |
|---|---|---|---|
| ① | 看門狗在 6.5s 後呼叫 `ghost.forget()` | `forget()` 撤銷的是**使用者授權**；WebHID 的授權只能在**使用者手勢**裡重新取得，而 `syncBalls()` 走 `getDevices()`（只看得到還有授權的球）⇒ 中途撤銷之後**整段體驗都不可能自動接回** | 只在「還沒開始體驗」（`state.phase === "before" && arrival.step === "connect"`）才撤銷；其餘情況保留授權並持續嘗試自動接回 |
| ② | 問卷的開窗閘門（`!a.armed`）**沒有任何逾時** | 球凍在高位 ⇒ 永遠等不到「放開」。`arrival` 的同一個閘門 2026-07-29 就補過 `AGREEMENT_ARM_GRACE_MS`，**問卷漏了** | `AFTER_ARM_GRACE_MS = 4000`，逾時就開窗，但記下殘壓當地板 |
| ③ | 空白鍵/Shift 後援的條件是 `!state.connected[slot]` | 球靜掉之後 `connected` 還是 true（看門狗要 6.5s 才清）⇒ 那段時間**連鍵盤退路都沒有** | 新增 `slotLive(slot)`（connected ＋ ready ＋ 最後一次 report 在 `HID_STALE_MS` 內），五處後援與畫面提示都改用它 |

> **為什麼 4-7-8 是最容易踩到的一段**：自走化之後（Pan 2026-08-04「就不抓取使用者的捏握了」）
> 使用者有整整 **104 秒**（26.1s × 4 輪）完全不需要出力——正好是球最容易進省電/靜默的一段。
> 換句話說，②③ 一直都在，是 ①＋「104 秒不需出力」把它們一起引爆的。
>
> ⚠️ **「不卡住」不可以用「編一個分數」換來。** 這是這次修法最容易做壞的地方，而且做壞了
> **測起來仍然是「問卷跑得完 4/4」**，差別只在分數被記成什麼。我自己先寫錯了兩種形狀：
> ① **地板取開窗那一刻的瞬時值**：畫面沒反應時使用者本來就會用力試握，如果剛好在開窗
> 那一刻正在握，就會把**使用者的答案本身**記成殘壓地板 ⇒ 那一題永遠 0 分
> （實測：殘壓 700raw ＋ 真的握到 1400raw ⇒ 記成 0）。
> 改成取**等待期間看到過的最低水位**，並且地板**只會往下修**（殘壓自己退掉之後門檻要跟著回到正常）。
> ② **回應窗仍從 `a.questionAt` 算**：開窗保險吃掉的 4000ms 是從 5200ms 的回應窗裡扣的
> ⇒ 只剩 1200ms 可以完成 1100ms 的 hold。實測**反應時間 ≥1500ms 的人真的握了也被記成 0**。
> 改成 `windowFrom = max(questionAt, armedAt)`——與 `arrival` 的 `agreeArmedAt`
> （2026-07-29 同一個理由）一致；正常球沒有開窗延遲，行為完全不變。
>
> 修好之後：殘壓 700raw 下反應 200–4000ms 都記得到分、球正常那條路不變，而
> 「殘壓不動」「完全不握」「1400raw 貼頂不動」三種情況仍然一律記 0（沒作答）＝**不編分數**。
> 驗收在 `tmp/test_survey_recovery.js`（119 項，兩頁各跑一次，把**真正的** `afterSurveyStep`
> 抽出來餵假時鐘）＋ `tmp/mutate_survey_recovery.js`（56/56）。
> ⚠️ 測試**必須**抽真函式來跑：診斷 AF 的過程中我的 probe 自己重寫了一份邏輯、又用「不看
> 提示就握」的時程，量出來的一整排 0 分全是 probe 的假象——差點照著假象去改對的程式碼。
> ⚠️ 斷言「某個呼叫在不在」時要**先把註解剝掉**：這一段的註解本身就寫著
> `ghost.forget()` 與 `syncBalls()`（在解釋為什麼不呼叫前者），不剝的話註解會替程式碼作證＝斷言恆真。

### 7.0.1 接手警示：Arrival 校正（Pan 2026-07-15）

> 更新（Claude 2026-07-15）：那個「稍微碰到就下一階」的過度敏感版本已 **revert 回 0dbffa0**（時間節奏 cue）。以下是未來要正確重做 per-ball 校正的方向，**不要再用降全域門檻的方式**。

Codex 那版為了讓 input 偏小的球更容易觸發，把校正門檻降得太低，結果變成「稍微碰到就下一階」。這違背 Arrival 的目的：它不是跳過校正，而是讓系統用三次舒適握壓認識每顆球與每隻手。

下一輪請優先重做這段：

- 左手三次與右手三次都必須完整完成；不能因為一顆球有輸入就跳過另一手。
- cue 成功條件應包含「有意圖的握起」與「短暫穩定 hold」，不應只看單點超過低門檻或瞬間 rising delta。
- 每次成功後更新該顆球的相對 0、舒適握壓範圍、release threshold。三次應逐次改善，不是三個動畫步驟。
- 30 秒呼吸覺察也要依校正結果判定吸握/吐放的狀態變化；同一個呼吸狀態內的抖動或兩手近乎同時變化，需要 time window 與 state hold 避免重複計數。
- 鍵盤 demo（空白鍵/Shift）仍需保留，因為沒有握力球時也要能展示完整流程。

### 7.1 中央圓球與握力物理（Pan 決策 2026-07-14）

中央圓球不是能量條；它是被手塑形的內核。**握力越大，球越小**，因為球被擠壓。光、水位、低頻與海潮能量可以隨握力增強，但幾何尺度要收縮，呈現「壓縮、變密」而不是「越用力越膨脹」。

### 7.2 4-7-8 水中石頭 × 梆子節律（Pan 決策 2026-08-04，取代 7-14 的握拍版）

**⚠️ 這一節在 2026-08-04 被 Pan 明確改掉了。4-7-8 現在不抓取使用者的捏握。**
下面先寫現行設計，舊的握拍版留在最後當歷史紀錄。

**依據**：Pan 指定參考 *The calming effect of a new wearable device during the
anticipation of public speech*（2016）。該研究的機制是——穿戴裝置給出一個**比使用者
當下呼吸更慢**的觸覺節律，靠被動同步（passive entrainment）降低預期焦慮。
關鍵在「比使用者慢」與「被動」：使用者不需要做對任何事，節律自己走。
這同時支持了 Pan 的兩個決定：**節律形狀由裝置給定**，以及**不抓捏握**。

**形式**：中國戲曲的**梆子打法**——一記重音（板）＋一串逐漸變弱的點（眼）。
每一段的敲擊總數等於該段的數字（`n = count − 1` 個點，加上開頭那一記板）。

**速度：等速（Pan 決策 2026-08-06，撤回了 08-05 的漸快漸慢）**
Pan 試用漸快漸慢版之後說：「**現在我覺得 478 還是用規律數數跟震動就好**」。所以
`BANGZI_CURVE` / `BANGZI_CURVE_MIN` / `BANGZI_CURVE_MAX` / `bangziGaps()` 全部移除，
拍距一律等於 `BANGZI_UNIT_MS`。

這**不違背**上面那篇 2016 年的依據：該研究要的機制是「比使用者當下呼吸**慢**」，
是一個**速度水準**，不是「速度會變」。等速 2.3 次／分照樣遠慢於靜息 12–16。
（反過來說，08-05 的漸快漸慢是把戲曲的表情放進來，那是美學選擇，不是機制要求——
Pan 試聽之後不要，就沒有理由留著。）

| 段 | 板 | 眼 | 拍距（ms） | 段長 |
|---|---|---|---|---|
| 4 | 1 記重音 | 3 點 | 1374（全部相同） | 5.5s |
| 7 | 1 記重音 | 6 點 | 1374 | 9.6s |
| 8 | 1 記重音 | 7 點 | 1374 | 11.0s |

等速的附帶效果是段長比例變成**精確的 1.00 : 1.75 : 2.00**——因為段長就是
`count × UNIT`，而 4 : 7 : 8 本身就是那個比例。漸快漸慢那版只能逼近（1 : 1.56 : 1.75）。

一輪 26.1s ＝ 19 拍 ＝ **2.3 次呼吸／分**；4 輪 104s（≈1.7 分鐘）——與 08-05 版**完全同長**，
因為 `BANGZI_UNIT_MS` 從 880 改成 **1374** 就是為了維持這個總長（19 × 1374 ≈ 26.1s）。
⚠️ 1374 這個數字取代了 Pan 自己 08-05 的 MIDI 決定，所以 MIDI 的分析結果**沒有丟掉**，
保留在 `web/index.html` 那一節的註解與 `tmp/sim_bangzi_478.js` 的檔頭裡（format 0、
96 tpq、11 音、IOI `116 76 73 57 57 62 76 78 96 108` tick、力度
`127 70 63 52 57 63 65 61 57 50 39`），要復原有據可循。

其餘常數不變：`BANGZI_ACCENT = { intensity: 165, duration: 78 }`、
`BANGZI_TICK_MIN/MAX = 62/118`（眼的強度仍然**逐點遞減**——等速指的是拍距，
不是力度；收尾的淡出留著）、**`BANGZI_TICK_MS = 54`**（眼的震動時長）。後者是
Pan 2026-08-05 回報「有些數字有震動 有些沒有」加的：原本寫死 34ms，比
`GRIPBALL_PROTOCOL.md:73` 的範例（50ms）還短，短到一部分點在真球上感覺不到。54ms 仍明顯
短於板的 78ms（板還是重音），而拍距 1374ms 遠大於 `sendHaptic` 的 95ms 節流窗，所以不會有
點被吃掉（`tmp/sim_bangzi_478.js [8]` 有斷言）。`BANGZI_SLOWDOWN` 與 `BANGZI_LEAD_MS`
在 08-05 就已移除。
（段落聽起來的「數字」仍然是石頭的**數量**，不是秒數。）

**聲音**：`underwaterStone(azimuth, distance, intensity, size)` ——圓石在水中互撞的低沉聲。
`f0 = 190 − 70·size`、四個模態 `decays = [0.45, 0.26, 0.16, 0.10]`、整體過 900Hz 低通
（「被水吃掉高頻」）、`peak = clamp(intensity, 0.15, 1) × 0.30`（守限幅保護）。

> **更新（Pan 2026-08-05）：每一段的第一個音（板）改用頌缽，其餘的點維持水中石頭。**
> Pan：「現在倒數這邊的聲音每段第一個音可以用頌缽的聲音嗎 其他聲音維持目前設定沒關係」。
> 分岔看 `p.accent`（＝時間表上的板），不看第幾次呼叫、更不看使用者輸入。
> 這與上面「頌缽的長衰減會讓連擊糊成一團」不衝突：一段裡只有**一記**板、段長 6.0–10.6s，
> 而點是低沉的水中石頭、頻率完全分開（缽 f0 ~300Hz 起、石頭 190 − 70·size）。
> `singingBowl` 自己有 0.35s 冷卻，而板與板至少隔一整段，不會被冷卻吃掉。
> 驗收在 `tmp/sim_bangzi_478.js [4c]③` 與 `[9]`（斷言「一輪剛好三記缽、其餘全是石頭」，
> 而不只是「碼裡有 singingBowl」）。

**倒數的語意（Pan 2026-08-05 修正）**：畫面數字＝「還剩幾下要聽」，而**正在響的這一下還沒
過去**。所以從時間表反推時要扣掉當下那一記：`remaining = count − max(0, done − 1)`。
少了那個 −1，板的 `at = 0` 在 `elapsed = 0` 就成立、被算成「已經打完」⇒ 第一眼看到的是
`count − 1`，這正是 Pan 回報的「例如 7 這段 竟然是從6開始」。
⚠️ 這個 bug 曾經**騙過**一條看起來對的斷言（`maxRemaining.hold === 7`）：`startBangziPhase`
確實有把 `remaining` 設成 7，只是下一幀就被蓋掉，所以 7 只存在一幀＝畫面上讀不到。
現在的斷言量的是**每個數字顯示了多久**（≥400ms 才算讀得到）＋**數字換的時刻要落在某一記的
時間點上**（畫面要跟耳朵對齊，不是按剩餘時間等速跑）。

**震動**：這是 `HAPTICS_ENABLED = false` 這條 guardrail 的**唯一例外**，Pan 2026-08-04
明確要求。只用**顯式 `force`** 呼叫、且只綁在**時間表**上，絕不看使用者輸入——
「震動絕不用作懲罰或催促」仍然完全有效。`tmp/sim_bangzi_478.js [8]` 就是在釘這件事
（有一個變異專門模擬「梆子的震動改看使用者握壓」＝催促，必須被抓到）。

> **哪些拍要震（Pan 2026-08-06）**：「**除了開頭那一下頌缽不要震動之外每一下都要震動**」。
> 也就是 `if(!p.accent) sendHapticAll(...)`——板（頌缽）只有聲音、不震；眼全部都震。
> 一輪 19 拍裡有 **16 拍會震**，3 記缽對手是安靜的。
> ⚠️ 只拿掉**震動**，缽的**聲音**要留著（`p.accent` 那一支仍呼叫 `singingBowl`）。
> 這兩件事很容易一起改掉，所以 `tmp/mutate_bangzi_478.js` 有五個變異守著：
> 板又跟著震、條件寫反（只有板震）、震動整個拿掉、板連聲音一起拿掉、震動改看握壓。
> 驗收數字在 `tmp/sim_bangzi_478.js [4c]②`：`hapticCount === beats − accents`
> 且 `stoneCount + bowlCount === beats`。

**推進方式**：時間驅動的狀態機（`resetManual478` / `startBangziPhase` / `tickManual478` /
`beginManual478` / `trigger478Press`），每幀 `tickManual478()`。開始之後握壓沒有任何作用。

**保持原狀的部分**（Pan 明確指定）：**抵達流程的問問題**（`ARRIVAL_PRESS_ON`）與
**前面的自由／共振呼吸**（`presetId === "resonance"` 的引導）都不動。

> **歷史（Pan 決策 2026-07-14，已被上面取代）**：原本每段要使用者自己握拍數數
> （吸氣 4→1、憋氣 7→1、吐氣 8→1，每次有效握壓扣一），每握一下給一次短觸覺確認。
> 預留的語音引導檔 `web/audio/478-{inhale,hold,exhale}.wav` 仍未放入（缺檔不應破壞流程）。

### 7.3 節奏分析欄位（建議新增）

為了讓 Tidal 能累積「節奏是否被調和」的證據，session/CSV 建議新增：

| 欄位 | 類型 | 說明 |
|---|---|---|
| `felt_pushed` | 0–10 | 這段過程有多像「被節奏推著走」。 |
| `felt_able_to_stay` | 0–10 | 有多能停留、呼吸、觀察。 |
| `agency` | 0–10 | 聲音是否像可被自己塑形，而非被系統控制。 |
| `rhythm_note` | text | 一句話：哪一刻最緊？哪一刻最鬆？ |
| `dominant_mode` | enum | 研究/分析用：poly/eu/arry/iso/unknown；不要直接顯示給使用者。 |

## 8. 明確的非目標

- 不做自由生成音樂、不即時作曲。
- 不做情緒/狀態診斷、不宣稱療效。
- 不把消費級 EEG 設為 day-one 必要條件。
- 不追求電影級擬真海浪；追求的是**可安住、可被雙手塑形、放鬆導向**的聲景。
